# 10分钟快速学会后端开发和部署

## 🚀 第一步：本地开发（2分钟）

1. 安装依赖
```bash
npm install
```

2. 启动服务器
```bash
npm start
```

3. 访问 http://localhost:3000 测试

## 📝 核心概念（3分钟）

### 后端是什么？
- 处理 HTTP 请求的服务器程序
- 提供 API 接口给前端调用

### 主要技术
- **Express**: 最流行的 Node.js 框架
- **路由**: `app.get('/path', handler)` - 定义不同的接口路径
- **请求/响应**: `req` 是请求，`res` 是响应
- **JSON**: 前后端数据交换的标准格式

### 文件结构
```
server.js          # 主服务器文件
package.json       # 项目配置和依赖
```

## ☁️ 部署（5分钟）

### 方案一：Vercel（免费）

1. 推送到 GitHub
```bash
git init
git add .
git commit -m "初始提交"
git remote add origin <你的仓库地址>
git push -u origin main
```

2. 去 vercel.com 导入项目

### 方案二：Railway（免费）

1. 同样先推送到 GitHub
2. railway.app 导入，自动部署

### 方案三：传统服务器

```bash
# 服务器上运行
npm install
NODE_ENV=production npm start
```

## 🔗 可用接口

- `GET /` - 首页信息
- `GET /api/tasks` - 获取任务列表
- `POST /api/tasks` - 创建新任务

## 总结
1. 开发：Express + Node.js
2. 部署：Git + Vercel/Railway
3. 测试：浏览器或 Postman
